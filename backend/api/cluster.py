from backend.cluster_models import ClusteringAlgo
from utils.data_loader import load_data
from typing import List

def perform_clustering(method : str, params : List[str]):
    # df, scaled = load_data()
    # clusters = ClusteringAlgo(scaled)
    pass
    